import React from 'react';
import './index.scss';
const CustomIconArea = ({ children }: { children: React.ReactNode }) => {
  return <div className="custom-icon">{children}</div>;
};

export default CustomIconArea;
